import { seedDatabase } from '../../../tests/test-seed'
import { notifications } from './methods'

describe('notifications methods', function () {
    it('', function () {
        seedDatabase()
        // Meteor.call(notifications.sendNotification)
    })
})
