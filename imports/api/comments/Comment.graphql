type Comment {
  _id: String
  text: String
  # likes: Integer;
  postedBy: User
}

extend type Mutation {
    createComment(text: String!, postId: String!): Feed
}

extend type Query {
  comments: [Comment]
}